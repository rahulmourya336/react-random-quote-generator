# dependencies
node_modules

# build output
dist
dist-ssr
build
*.local

# testing
coverage

# environment
.env.local
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editors / os
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea

# Claude Code skills are vendored copies of other people's repos and ship no
# licence, so they are not redistributed here. skills-lock.json records the
# source, path and hash of each, which is what makes them reinstallable.
.claude/skills/
.agents/
# Playwright CLI output (may contain credentials)
.playwright-cli/
